[kunpanel-auth]
enabled = true
backend = systemd
journalmatch = _SYSTEMD_UNIT=kunpanel.service + _SYSTEMD_UNIT=tryallfun-panel.service
filter = kunpanel-auth
port = http,https,8088
findtime = 10m
maxretry = 5
bantime = 1h
banaction = nftables-multiport

